[pytest]
markers =
    real_scrypt: use the production scrypt parameters instead of the cheap test profile
    fresh_install: start on a brand-new install (setup not completed) instead of a set-up one
